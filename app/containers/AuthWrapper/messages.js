/*
 * AuthWrapper Messages
 *
 * This contains all the text for the AuthWrapper component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.AuthWrapper.header',
    defaultMessage: 'This is AuthWrapper container !',
  },
});
