// import { selectUserScreenshotsDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectUserScreenshotsDomain();

describe('selectUserScreenshotsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
